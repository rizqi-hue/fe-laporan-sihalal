import React, { FC } from "react";

export interface RegisterProps {
  className?: string;
}

const Register: FC<RegisterProps> = ({ className = "" }) => {
  return <>Register</>;
};

export default Register;
